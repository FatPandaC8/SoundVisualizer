package main

import (
	// "fmt"
	// "math/cmplx"
	// "shazam/internal"
	// "log"
	// "shazam/internal"
)


func main() {
	// _, err := internal.ReadWav("songs/Uoc_Mua.wav");
	// if err != nil {
	// 	log.Fatal(err)
	// }
	// 8-point square wave: [1,1,1,1,0,0,0,0]
	// signal := []complex128{1, 1, 1, 1, 0, 0, 0, 0}
	
	// spectrum := internal.FFT(signal)
	
	// fmt.Println("Frequency Domain:")
	// for i, val := range spectrum {
	// 	magnitude := cmplx.Abs(val)
	// 	phase := cmplx.Phase(val)
	// 	fmt.Printf("Bin %d: Magnitude=%.2f, Phase=%.2f rad\n", 
	// 		i, magnitude, phase)
	// }
}

